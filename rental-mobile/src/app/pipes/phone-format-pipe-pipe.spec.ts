import { PhoneFormatPipePipe } from './phone-format-pipe-pipe';

describe('PhoneFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
